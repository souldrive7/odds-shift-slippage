# Versions used to compute every result file in results/ (Python 3.12.10, Windows, CPU only).
# Byte-identical JSON needs these pins. Nothing here records the versions of any
# individual run, so a different minor version is untested rather than known-equivalent.
numpy==2.4.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.9
lightgbm==4.6.0
xgboost==3.2.0
# CPU build is sufficient (the MLP learner is small):  pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

# Only needed to run the shipped tests and to rebuild the oddslip tutorial notebook.
pytest==9.0.3
nbformat==5.10.4
nbclient==0.10.4
